            Linux 系統架構
Linux 可分為四個主要層次：
+----------------------+
|   Application        |
| (Chrome、VS Code)    |
+----------------------+
|        Shell         |
|   Bash、Zsh          |
+----------------------+
|   Linux Kernel       |
+----------------------+
| Hardware             |
| CPU、RAM、Disk       |
+----------------------+
各層功能如下：

硬體層（Hardware）

包含 CPU、記憶體、硬碟、滑鼠、鍵盤及網路卡等設備，是所有程式執行的基礎。

Linux Kernel

核心負責管理所有硬體資源，包括程序排程、記憶體管理、檔案系統及裝置驅動。

Shell

Shell 是使用者與作業系統溝通的介面，接收指令後，透過系統呼叫交由 Kernel 執行。

Application

應用程式（例如瀏覽器、IDE、資料庫）透過作業系統提供的 API 存取硬體資源。